import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1
  },
  loader: {
    height: 200,
    alignItems: 'center',
    justifyContent: 'center'
  }
});
